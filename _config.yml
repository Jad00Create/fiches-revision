title: "Mémorise - Fiches de Révision"
description: "Génère des fiches de révision et flashcards avec l'IA — Maths, Physique, NSI et plus encore"
lang: fr
theme: jekyll-theme-cayman

plugins:
  - jekyll-remote-theme

defaults:
  - scope:
      path: ""
    values:
      layout: default

exclude:
  - node_modules
  - vendor
  - .git
